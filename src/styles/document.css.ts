import { style } from "@vanilla-extract/css";
import { sprinkles } from "@css-utils";

export const DocumentStyles = sprinkles({
  background: { darkMode: "black" },
  color: { darkMode: "white" },
});
